[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Let React Router handle deep links such as /admin/login and /access.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
